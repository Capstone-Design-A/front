/*
import React from "react";
import styles from "./ProductIcon.module.css";

function ProductImgae({ imageUrl, altText }) {
  return <img className={styles.icon} src={imageUrl} alt={altText} />;
}

export default ProductImgae;
*/
